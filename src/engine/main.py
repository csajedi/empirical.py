from jug import TaskGenerator

""" This class contains the runtime for calculating the tape, reducing its results and creating a search-optimized datastructure."""


# Use the enumeration engine to determine the size of the enumeration set

# Get some chunk of the unadvanced enumerations